#ifndef CPPI_OPTIONS_HPP
#define CPPI_OPTIONS_HPP

#include <vector>
#include <string>

namespace cppi {

struct options {
    std::vector<std::string> include_dirs;
};

} // cppi


#endif
